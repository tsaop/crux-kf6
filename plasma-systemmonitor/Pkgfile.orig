# Description: An interface for monitoring system sensors, process information and other system resources
# URL: https://invent.kde.org/plasma/plasma-systemmonitor
# Maintainer: Tsaop, leeroy at cock dot li
# Depends on: attica brotli dbus double-conversion fontconfig freetype glib harfbuzz karchive kauth kcodecs kconfig kcoreaddons kcrash kdbusaddons kglobalaccel ki18n kio kirigami-addons kitemmodels knewstuff kpackage kservice ksystemstats kwindowsystem libb2 libglvnd libksysguard libpng libxkbcommon lm_sensors qt6-base qt6-declarative solid syndication xorg-libx11 xorg-libxau xorg-libxcb xorg-libxdmcp xorg-libxfixes xorg-xcb-util-keysyms

name=plasma-systemmonitor
version=6.0.1
release=1
source=(https://download.kde.org/stable/plasma/$version/$name-$version.tar.xz)

build() {
	cd $name-$version
	cmake -Bbuild \
		-DCMAKE_INSTALL_PREFIX=/usr \
		-DCMAKE_INSTALL_LIBDIR=lib

	cmake --build build
	DESTDIR=$PKG cmake --install build

	rm -rf $PKG/usr/share/locale
}
